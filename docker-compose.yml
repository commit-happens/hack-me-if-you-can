services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hack_me_if_you_can
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: your_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
